<?xml version="1.0" encoding="utf-8"?>
<!-- 
    ملف التخطيط الرئيسي لتطبيق OneUI المحسن
    يدعم جميع الميزات المتقدمة مثل Pull-to-Reach والشريط القابل للطي
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <!-- المحتوى الرئيسي مع CoordinatorLayout لدعم التفاعلات المعقدة -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <!-- AppBarLayout يحتوي على شريط الأدوات القابل للطي -->
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="280dp"
            android:fitsSystemWindows="true"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

            <!-- CollapsingToolbarLayout مع ميزات OneUI المحسنة -->
            <com.google.android.material.appbar.CollapsingToolbarLayout
                android:id="@+id/collapsing_toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fitsSystemWindows="true"
                app:contentScrim="?attr/colorPrimary"
                app:statusBarScrim="?attr/colorPrimaryVariant"
                app:layout_scrollFlags="scroll|exitUntilCollapsed|snap"
                app:toolbarId="@+id/toolbar"
                app:expandedTitleMarginStart="16dp"
                app:expandedTitleMarginBottom="32dp"
                app:expandedTitleTextAppearance="@style/TextAppearance.OneUI.CollapsingToolbar.Expanded"
                app:collapsedTitleTextAppearance="@style/TextAppearance.OneUI.CollapsingToolbar.Collapsed"
                app:expandedTitleGravity="bottom|start"
                app:collapsedTitleGravity="start"
                app:extendedTitleEnabled="true"
                app:extendedSubtitleTextAppearance="@style/TextAppearance.OneUI.CollapsingToolbar.Subtitle">

                <!-- خلفية متدرجة مع تأثير OneUI الأصلي -->
                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/oneui_header_background"
                    android:scaleType="centerCrop"
                    android:fitsSystemWindows="true"
                    app:layout_collapseMode="parallax"
                    app:layout_collapseParallaxMultiplier="0.7" />

                <!-- طبقة تدرج شفافة للتأكد من وضوح النص -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/oneui_header_overlay"
                    app:layout_collapseMode="parallax" />

                <!-- شريط الأدوات الرئيسي -->
                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:elevation="0dp"
                    app:layout_collapseMode="pin"
                    app:contentInsetStartWithNavigation="0dp"
                    app:navigationIcon="@drawable/oneui_ic_menu"
                    app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
                    app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

            </com.google.android.material.appbar.CollapsingToolbarLayout>

        </com.google.android.material.appbar.AppBarLayout>

        <!-- SwipeRefreshLayout للسحب لأسفل للتحديث -->
        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipe_refresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <!-- NestedScrollView للتمرير السلس مع AppBarLayout -->
            <androidx.core.widget.NestedScrollView
                android:id="@+id/nested_scroll"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fillViewport="true"
                android:scrollbars="vertical"
                android:overScrollMode="never"
                android:clipToPadding="false">

                <!-- الحاوي الرئيسي للمحتوى والأجزاء -->
                <FrameLayout
                    android:id="@+id/main_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="16dp"
                    android:minHeight="600dp"
                    android:background="@color/oneui_background_color">

                    <!-- هذا المكان سيملأ ديناميكياً من MainActivity.java -->
                    <!-- المحتوى سيتم إضافته برمجياً -->

                </FrameLayout>

            </androidx.core.widget.NestedScrollView>

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <!-- Floating Action Button اختياري لميزات إضافية -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_main"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/oneui_ic_add"
            android:visibility="gone"
            app:tint="@android:color/white"
            app:backgroundTint="?attr/colorAccent"
            app:layout_behavior="dev.oneuiproject.oneui.design.behavior.HideBottomViewOnScrollBehavior" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Navigation Drawer اختياري للتنقل الجانبي -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:visibility="gone"
        app:headerLayout="@layout/drawer_header"
        app:menu="@menu/drawer_menu"
        app:theme="@style/OneUINavigationView" />

</androidx.drawerlayout.widget.DrawerLayout>
